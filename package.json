{
  "name": "sonic-online-race",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Four-player, asynchronous Sonic 1 race rooms with live player ghosts.",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check public/config.js && node --check public/js/app.js && node --check public/js/device.js && node --check public/js/emulator.js && node --check public/js/network.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.3"
  }
}
