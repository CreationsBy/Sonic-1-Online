{
  "name": "sonic-1-online",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Four-player cooperative Sonic 1 rooms with live teammate ghosts and spectating.",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check public/config.js && node --check public/js/app.js && node --check public/js/device.js && node --check public/js/emulator.js && node --check public/js/network.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.3"
  }
}
